{
  "name": "hide-or-hack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "tunnel": "npx localtunnel --port 3000"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "three": "^0.163.0"
  },
  "engines": {
    "node": ">=18"
  }
}

